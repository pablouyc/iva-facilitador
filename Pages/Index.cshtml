@page
@model IvaFacilitador.Pages.IndexModel
@{
    ViewData["Title"] = "Inicio";
    Layout = "/Pages/Shared/_Layout.cshtml";
}
<div class="row justify-content-center">
  <div class="col-lg-10">
    <div class="card shadow-sm card-iva">
      <div class="card-body p-4">
        <h1 class="h4 mb-3">Inicio</h1>
        <p class="text-secondary">Hola, bienvenido <strong>@(User?.Identity?.Name ?? "usuario")</strong>.</p>
        <p class="mb-0">Utiliza el menú para ir a <a href="/IVA/Seleccion">Cálculo de IVA</a> o gestiona tus <a href="/Empresas">Empresas</a>.</p>
      </div>
    </div>
  </div>
</div>
